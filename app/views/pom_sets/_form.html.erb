<%= form_for(@pom_set) do |f| %>
  <% if @pom_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pom_set.errors.count, "error") %> prohibited this pom_set from being saved:</h2>

      <ul>
      <% @pom_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
